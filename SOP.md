# ATLAS OS — 운영 SOP (마스터)

> **이 문서는 프로젝트 knowledge에 상주한다.** 따라서 이 프로젝트의 **모든 대화가 시작부터 이 문서를 자동으로 읽는다** — 새 대화마다 SOP를 수동 첨부할 필요가 없다. 이것이 "대화 간 기억 비공유" 문제의 자동 해결책이다.
>
> **엔진(대화 상대)에게:** 이 프로젝트에서 작업할 때 이 SOP의 절차·하드룰을 기본 전제로 삼는다. 사용자가 최신 `ATLAS_PACK.json`만 첨부하면 곧바로 갱신/조회를 수행한다.
>
> **사용자에게:** 갱신·조회 시 **최신 `data/ATLAS_PACK.json`만 첨부**하면 된다(딥다이브가 있으면 그 팩도 함께). SOP는 이미 상주 중이라 다시 붙일 필요 없다. *예외:* 이 프로젝트 밖의 대화라면 knowledge가 없으므로 그때만 `SOP.md`도 첨부한다.

## 문서 지형 (역할 경계 — 충돌 방지)

이 프로젝트의 지침 문서는 역할이 겹치지 않게 분리돼 있다:

| 문서 | 관장 | 질문 |
|---|---|---|
| `00 / 01 / 02` | **판단 로직** (분류·채점·티어·전파·거버넌스) | "무엇을 어떻게 판단하나" |
| **`SOP.md`** (이 문서) | **운영·배포 절차** (트리거·산출물·깃허브·폴더트리·하드룰) | "어떻게 돌리고 반영하나" |
| `ATLAS_USAGE.md` | **활용 가이드** (요청 가능한 작업·예시·경계) | "뭘 어디까지 물어보나" |

충돌 시: **판단 기준은 00/01/02가, 절차는 이 SOP가 관장**한다(02 거버넌스와 동일한 레이어 원칙).

## 문서 갱신 규칙 (버전 드리프트 방지 — v1.4 확장)

**대상: 지침 문서 6종 전부** (`00`·`01`·`02`·`03`·`SOP.md`·`ATLAS_USAGE.md`).

- **레포가 원본, 프로젝트 knowledge의 것은 그 사본**이다. 6종 모두 레포 루트에 상주한다.
  (v1.4 이전에는 레포에 `SOP.md`만 있어 판단 로직 문서 4종에 git 이력이 없었다 — 해소됨.)
- 지침 문서를 고칠 때는 **레포와 knowledge를 같은 세션 안에서 함께 갱신**한다.
  한쪽만 고치면 다음 세션이 어느 쪽을 원본으로 읽을지 알 수 없다.
- 갱신 후 하단 §7 버전 이력에 한 줄 남긴다.
- **동기화 검증:** 지침 문서를 고친 세션은 종료 전 레포본과 knowledge본의 md5 일치를
  확인한다. PACK에 적용하는 규율(§4-1)을 지침 문서에도 동일하게 적용한다.

**드리프트 실사례 (2026-09-02 발견 — 이 규칙이 왜 필요한가):**
레포 `SOP.md`가 knowledge 상주 이전 버전(v1.3 이전)에 멈춰 있었고, 정작 그 안에
"레포가 원본"이라는 선언이 없었다. 원본으로 지정된 쪽이 더 낡아 있는 역전이 5개월치
누적됐다. 파일 개수가 아니라 **동기화 절차의 부재**가 원인이었다.

---

## 0. 시스템 한 눈에

- **지도의 유일한 원본 = `data/ATLAS_PACK.json` (단 하나).**
- 뷰 4개(콘솔·관계망·히트맵·가이드)는 **전부 이 PACK을 fetch**해서 그 자리에서 화면을 만든다.
  → **매 갱신 = PACK 하나만 교체하면 세 데이터 뷰가 동시에 갱신된다.** (가이드는 정적 설명서)
- `data/deepdive/` = 개별 딥다이브 원본 **아카이브**(사이트는 안 읽음, 출처 보관용).
- 파생 파일(graph_data.json 등)은 **없다.** 파생은 각 HTML 안(`ATLAS_DERIVE`)에서 브라우저가 즉석 수행.

---

## 1. 최종 폴더 트리 (확정)

```
atlas_site/
├─ index.html              진입점 → 콘솔로 자동 이동
├─ ATLAS_CONSOLE.html      뷰1 · 섹터 브라우저 + 필터 + 드릴다운   ┐
├─ ATLAS_GRAPH.html        뷰2 · 관계망(엣지 따라 탐색)            │ 셋 다 data/ATLAS_PACK.json 을 fetch
├─ ATLAS_MAP.html          뷰3 · 히트맵(인쇄·공유용)               ┘ → 코드 수정 불필요, PACK만 갈면 갱신
├─ ATLAS_GUIDE.html        뷰4 · 사용 가이드(정적)
├─ README.md               저장소 설명
│
│  ── 지침 문서 6종 (원본. knowledge의 것은 사본) ────────────────
├─ 00_ATLAS_OS.md          미션·상태파일 원칙·3개 모드·금지사항      ┐
├─ 01_ATLAS_TAXONOMY_SCORING.md  분류·산업카드·트리거·티어·PACK스펙  │ 판단 로직
├─ 02_ATLAS_BUILD_GOVERNANCE.md  BUILD 증분 구축 규율                │ (00/01/02)
├─ 03_ATLAS_UPDATE_GOVERNANCE.md UPDATE 세션 규율                   ┘
├─ SOP.md                  ★ 이 문서 (운영·배포 절차)
├─ ATLAS_USAGE.md          활용 가이드 (무엇을 어디까지 요청하나)
│
└─ data/
    ├─ ATLAS_PACK.json      ★★ 유일한 원본. 매 갱신 시 이것만 덮어쓰기
    └─ deepdive/            개별 딥다이브 원본 아카이브 (사이트 미사용)
        ├─ README.md
        └─ <TICKER>_DATA_PACK_<YYMMDD>.json  × N
           (2026-09-02 기준 90파일 / 고유 86종 = PACK의 DEEP_DIVED 86종과 1:1 일치)
```

**트리 갱신 규율:** 딥다이브 아카이브는 개별 파일명을 여기 나열하지 않는다. 나열하면
갱신마다 트리가 낡는다(실제로 5개에서 멈춰 있었다). 개수와 규칙만 적고, 실물은 폴더가 원본이다.

---

## 2. 내가 OS 엔진에 요청할 수 있는 작업

| # | 작업 | 트리거(대화에 입력) | 첨부 | 산출물 | 깃허브 반영 |
|---|---|---|---|---|---|
| 1 | **월간/비정기 갱신** | `아틀라스 업데이트` | 최신 `ATLAS_PACK.json` (+ 완료된 딥다이브 팩) | 새 `ATLAS_PACK.json` · `ATLAS_DIFF_YYMMDD.md` | `data/ATLAS_PACK.json` 교체 (+ 딥다이브 팩은 `data/deepdive/`) |
| 2 | **조회·질문** | 자유 질문 | 최신 `ATLAS_PACK.json` | 대화 답변 (파일 없음) | 없음 |
| 3 | **스크리닝 시작** | `스크리닝 시작 — <산업명>` | 해당 산업 카드(또는 PACK) | 스크리닝 후보 유니버스 (별도 공정) | 없음(딥다이브로 이어짐) |
| 4 | **최초 구축** | `아틀라스 구축 시작` | (없음/직전 PACK) | 블록별 PACK 누적 | 완료됨 — 재실행 불필요 |

> **딥다이브 자체**는 별도 OS(기업 검증 엔진)에서 수행되어 `<TICKER>_YYMMDD_DATA_PACK.json`을 낳는다.
> 그 결과가 **작업 #1의 첨부물**로 들어오면 아틀라스에 통합된다(아래 3-B).

### 2-A. 갱신(#1)이 하는 일 — 내부 순서

1. 첨부된 딥다이브 팩을 **노드 승격**(OS §7A): 기업 노드 `SHALLOW→DEEP_DIVED`, 밸류체인 편입, `deep_dive_ref` 저장.
2. 경량 스캔: 선행지표 변화 신호가 있는 산업만 심층 확인.
3. 전파(§3A, 최대 2홉) → 티어 재조정(트리거 증거 2개 이상).
4. 산출: 새 PACK + `ATLAS_DIFF_YYMMDD.md`(이번에 바뀐 것) + 필요 시 QA 게이트 통과 확인.

### 2-B. 딥다이브 결과를 아틀라스에 반영하려면

작업 #1을 부르면서 **그 기업 `<TICKER>_YYMMDD_DATA_PACK.json`을 함께 첨부**하면 된다.
그 기업이 "대장·유망주"가 아니어도 **제 밸류체인에 편입되어 확인 가능**하다:
- **콘솔** — 산업 드릴다운의 「밸류체인 편입 기업 · 커버리지」에 ✓검증 배지 + 판정(스탠스·thesis).
- **관계망** — 청록 검증 링 노드로 표시, 클릭 시 인스펙터에 deep_dive_ref 전체.
- **히트맵** — 산업 카드 기업 칩에 ✓ 표식.

---

## 3. 매 갱신 표준 절차 (체크리스트)

```
[대화]
 □ 최신 data/ATLAS_PACK.json 첨부  (SOP는 knowledge 상주 → 자동 적용, 재첨부 불필요)
 □ "아틀라스 업데이트" 입력 (+ 완료된 딥다이브 팩 첨부)
 □ 새 ATLAS_PACK.json 과 ATLAS_DIFF_YYMMDD.md 수령
[깃허브]
 □ data/ATLAS_PACK.json  ← 새 파일로 덮어쓰기
 □ (딥다이브 있었으면) data/deepdive/ 에 원본 팩 추가
 □ 커밋: "UPDATE-N YYMMDD: <핵심 변경>"  (예: ITC-08 NEUTRAL→WARM, 딥다이브 5종)
 □ (선택) ATLAS_DIFF 를 로컬/원하는 곳에 보관 (변경 이력)
[확인]
 □ GitHub Pages 1~2분 후 재배포 → 콘솔·관계망·히트맵 모두 최신인지 확인
 □ 프로젝트 지식의 PACK 도 최신본으로 교체 (다음 대화의 diff 기준)
```

---

## 4. 불변 규칙 (하드룰 — 어기면 품질/일관성 붕괴)

1. **원본은 `data/ATLAS_PACK.json` 하나.** 매번 **덮어쓴다.** 날짜별 PACK 사본을 `data/`에 쌓지 않는다(버전관리는 git 커밋 이력이 담당 = tier_history).
2. **직전 PACK을 반드시 첨부**하고 갱신한다. 없으면 diff 기준이 사라져 정확도가 급락한다.
3. **뷰 HTML 코드는 평소 건드리지 않는다.** PACK만 갈면 된다. (뷰 기능을 바꿀 때만 예외 — 그때는 3개 HTML을 교체하고 이후 다시 PACK-only로 복귀.)
4. **PACK 스키마는 파괴하지 않는다**(필드 추가 OK, 기존 필드/과거 데이터 삭제 금지). 뷰가 그 스키마에 의존한다.
5. **티어 = 조사 우선순위**이지 매매 신호가 아니다. 종목 판단·목표가 출력 금지. 딥다이브 스탠스는 별도 공정의 인용으로만 표기.
6. **품질 게이트**(갱신 시 자동 점검): HEAT ≤ 15%, 티어 변경마다 트리거 증거 2개+, 카드마다 선행지표 2개+·market_read 존재, COLD마다 바닥반전 신호, 엣지 mechanism 1문장, 전파 2홉 이내, pending_cross_edges PENDING 0.

---

## 5. 산출물 → 깃허브 매핑 (요약)

| 산출물 | 성격 | 어디에 |
|---|---|---|
| `ATLAS_PACK.json` | 유일한 원본 (사이트 구동) | `data/ATLAS_PACK.json` 덮어쓰기 |
| `<TICKER>_DATA_PACK_<YYMMDD>.json` | 딥다이브 원본 (아카이브) | `data/deepdive/` 추가 |
| `ATLAS_DIFF_YYMMDD.md` | 이번 변경 리포트 | 선택 보관(사이트 미사용) |
| 뷰 HTML (CONSOLE/GRAPH/MAP) | 렌더 코드 | 기능 변경 시에만 교체 |
| 지침 문서 6종 (`00`~`03`·`SOP`·`USAGE`) | 판단·절차 원본 | 레포 루트 교체 + knowledge 동시 교체 |

### 5-A. 딥다이브 아카이브 파일명 규칙 (v1.4 — 단일화)

```
<TICKER>_DATA_PACK_<YYMMDD>.json     ← 유일한 정규 형식
```
- 날짜는 **딥다이브 수행일 6자리(YYMMDD)**. 8자리(`20260824`)나 선행 날짜형
  (`ALB_260823_DATA_PACK.json`)은 레거시이며 신규 생성 금지.
- 티커는 언더스코어 1개로 구분한다(`NFLX__DATA_PACK`처럼 2개 금지).
- **분기 갱신으로 스탠스가 바뀌면 새 날짜 파일을 추가한다(덮어쓰지 않음).**
  단 내용이 동일하면 새 파일을 만들지 않는다 — 날짜만 다른 바이트 동일 사본은
  "언제 판단이 바뀌었나"를 오히려 흐린다. **변경이 있을 때만 새 날짜.**
- 레거시 파일명은 소급 개명하지 않는다(git 이력 보존). 신규분부터 규칙 적용.

---

## 6. 트러블슈팅 / FAQ

- **로컬에서 콘솔·관계망·히트맵이 안 열려요(빈 화면).** fetch는 `file://` 더블클릭에서 막힌다.
  폴더에서 `python3 -m http.server` 실행 후 `localhost:8000` 접속. GitHub Pages(https)에선 정상.
- **새 대화가 예전 맥락을 기억 못 해요.** 정상이다(대화 간 기억 비공유). 이 SOP가 **프로젝트 knowledge에 상주**하면 새 대화가 자동으로 읽으므로 해결된다 — 최신 PACK만 첨부하면 된다. (knowledge 등록 전이라면 SOP도 함께 첨부.)
- **딥다이브 원본을 사이트에서 바로 열고 싶어요.** 지금은 아카이브만. 원하면 콘솔/관계망에
  "전체 리포트 열기"(→ `data/deepdive/…`) 링크를 붙일 수 있다(선택 개발).
- **히트맵이 낡았어요.** 이제 히트맵도 PACK을 fetch하므로 PACK만 갈면 자동 최신화된다(2026-08 fetch 전환 완료).

---

## 7. 버전 이력

> **기록 규율:** 갱신 세션은 종료 전 여기에 한 줄을 남긴다. UPDATE-2~5 항목은
> 당시 기록이 누락되어 2026-09-02에 PACK(`meta`·`tier_history`·`monthly_diff`)에서
> **역추적한 요약**이다 — [추정] 태그로 구분한다(00 §4 증거 규율).

- **2026-08 (v1.3 / UPDATE-1)** — 최초 완성본(BUILD 1~7) 이후 첫 갱신. 딥다이브 5종 통합, ITC-08 NEUTRAL→WARM.
  뷰 업그레이드: 딥다이브 기업(역할 없어도) 밸류체인 가시화(콘솔 커버리지·✓검증·판정 / 관계망 검증 링 / 히트맵 ✓칩).
  히트맵을 fetch 기반으로 전환 → **세 뷰 모두 PACK 하나로 자동 갱신**.
- **2026-08-24 (UPDATE-2)** [추정] — B타입. CCJ·LEU 딥다이브 통합, UTL-02 HEAT 후보 등록(한도 포화로 WARM 유지).
- **2026-08-24 (UPDATE-3)** [추정] — B타입. VST 딥다이브 corroboration, UTL-02 HEAT 근거 강화.
- **2026-08 (UPDATE-4)** [추정] — B타입. 바텀업 딥다이브 21종 통합(승격 14 + 신설 7). 티어 변경 0.
- **2026-08 (UPDATE-5)** [추정] — **A타입 전면 스캔.** 56종 톱다운 재확인. §3A 아웃바운드 2홉 워크 실행(임계 통과 0) → 전파 부채 해소.
- **2026-08-30 (v2.2 / UPDATE-6)** — B타입 배치 1~6 완료. DEEP_DIVED 86종 도달. 티어 변경 0.
  거버넌스 `01` v1.3 REVISIT 트랙 승인 및 PACK 스키마 선반영(`type`/`trigger`/`status`), 후보 원장 배열화.
- **2026-09-02 (SOP v1.4 / 문서 정합화)** — **PACK 무변경.** 지침 문서 6종을 레포 루트로 승격(00~03·USAGE 신규 배치),
  레포 `SOP.md`를 knowledge 최신본과 동기화(역전 해소), §1 트리·§5 산출물 매핑·§5-A 파일명 규칙 사실 정정,
  문서 갱신 규칙을 6종 전체로 확장 + md5 동기화 검증 도입, §7 이력 백필.
  판단 로직(00~03 본문) 변경 0건.
