# 03_ATLAS_UPDATE_GOVERNANCE.md
## Global Sector Atlas OS v1.4 (Update-Session Discipline Layer)
## Role: 월간·수시 UPDATE 세션이 세션 간 일관성·신뢰성을 흘리지 않게 잠그는 규율
## Authority: 00/01 위에 얹히는 패치. 충돌 시 이 문서가 '절차'를, 00/01이 '판단 기준'을 관장. 02(BUILD 규율)의 자매 문서.

> 왜 존재하나: 갱신은 매번 새 대화에서 일어난다. 그 순간 세 곳에서
> 일관성이 샌다 — ① Claude의 memory 요약이 실제 repo·PACK 상태보다
> 부정확할 수 있는데 그걸 실물로 확인하지 않고 읊는다, ② 디테일(나무)에
> 파묻혀 아틀라스의 근본 목적(숲)을 작업 판단에서 놓친다, ③ 이번 세션이
> "무엇을 안 했는지"를 남기지 않아 다음 세션이 연속성을 잃는다.
> 이 문서는 세 누수를 절차로 막는다. **연속성을 Claude의 기억이 아니라 파일에 맡긴다.**

---

# 1. UPDATE 세션 개시 규율 (진입 게이트 — 순서 강제)

**핵심 재정의:** 갱신의 원본은 memory가 아니라 첨부된 PACK과 소스 파일이다.
Claude의 memory 요약은 보조일 뿐, 부정확할 수 있다. 실물로 확인하기 전에는
어떤 상태 주장도 하지 않는다.

규칙 — 아래 순서를 밟은 뒤에만 실제 갱신 작업에 착수한다:

1. **실물 확인 우선 (memory 금지).** 첨부 PACK을 먼저 로드해 `meta.version`·
   산업 수·노드 수·HEAT 목록·`monthly_diff`의 직전 인수인계를 **읽고 확인**한다.
   repo 구조·파일 위치는 사용자가 준 실물 트리로 확인하며, memory에서 읊지 않는다.
   (실패모드: repo 폴더 트리를 memory로 기억해 틀리게 안내 — 실제 발생함.)
2. **숲 대조 (tier/스키마/범위 건드리는 판단은 제안 前에).** 티어 의미론·PACK
   스키마·커버리지 범위를 바꾸는 결정은 **제안하기 전에** 00 §0 미션과 §2 원칙에
   대조한다. "이 산출물을 더 완전하게/엄밀하게"라는 국소 압력이 "얕지만 빠짐없이·
   티어=조사 우선순위"라는 상위 태도를 덮지 않는지 먼저 점검한다.
   (실패모드: 감사가능성을 위해 heat_score 영속화를 제안 — §2-b 위반이라 철회됨.)
3. **깊이 통합이면 전파를 명시적으로 판정한다.** 딥다이브 통합·노드 상태 변경이
   있으면 01 §3A 전파를 실행하거나, 이번 세션에서 어디까지 했는지(인바운드 재고 vs
   아웃바운드 2홉 워크)를 명시적으로 판정하고 §3에 부채로 기록한다. 조용히 건너뛰지 않는다.
4. **하드넘버는 원본 대조 후 반영.** 카드·`deep_dive_ref`·선행지표에 들어가는 수치는
   딥다이브 원본 팩과 대조한 값만 쓴다. QA 게이트는 구조만 검증하므로, 내용 충실도는
   이 대조로 메운다.
5. **마무리 前 부채 명시.** 세션 종료 전 "이번에 안 한 것"을 반드시 남긴다(§3).

---

# 2. 확정 원칙 (오늘의 판정을 박제 — 다음 세션도 동일하게 해석)

세션이 바뀌어도 아래 세 줄은 불변으로 해석한다. 이걸 놓치면 갱신이 옆길로 샌다.

- **(a) "얕지만 빠짐없이"의 '얕게'는 커버리지 폭이지 분석 깊이가 아니다.**
  56종을 빠짐없이 그물망에 올리되 다루는 축을 수급·사이클·변화율까지로 제한한다는
  뜻이다. 각 산업의 그 축에 대한 판단은 **깊고 정확해야** 한다. 깊이는 하류
  파이프라인(스크리닝·딥다이브)으로 병렬 조달하되, 결코 대충 보라는 말이 아니다.
- **(b) tier는 정성 판단이지 정량 스코어보드가 아니다.**
  tier = "어디를 더 조사할까"의 우선순위(00 §0). heat_score 등 구성점수를 **영속
  필드로 굳히지 않는다** — 티어를 매매 신호/정량 랭킹으로 reify하는 순간 OS 원칙과
  충돌한다. 15% 컷의 정성적 모호함(타이브레이크)은 버그가 아니라 설계다. 산업을 깊게
  보는 것과 티어를 느슨하게 두는 것은 완전히 양립하며, 오히려 깊게 봐야 정성 티어가
  신뢰할 만해진다.
- **(c) 넓음↔깊음은 아틀라스↔딥다이브 양방향 루프로 조달된다.**
  아틀라스가 위에서 "여기 데워진다"고 지목하면 → 스크리닝·딥다이브가 깊이를 만들고
  → 그 깊이가 §7A로 노드를 진하게 만들어 아틀라스를 갱신한다. 바텀업 딥다이브 팩을
  개별로 올리는 것은 이 루프의 "깊이 조달" 경로다. 깊이를 나중으로 미루라는 뜻이 아니라
  병렬로 조달해 상류로 되먹인다는 뜻이다.

---

# 3. 세션 인수인계 (연속성을 PACK에 둔다)

**문제:** 대화는 세션이 끝나면 휘발되고 memory 요약은 부정확할 수 있다.
연속성을 Claude의 기억에 맡기면 다음 세션이 길을 잃는다.

규칙:
1. 매 갱신 종료 시 `monthly_diff.coverage_honesty`(또는 `next_session_carry`)에
   **"다음 갱신이 이어서 할 일"**을 구조화해 남긴다. 남길 항목: 미실행 전파(§3A)
   범위, 재조사 안 한 산업 수, carry-forward peak_watch와 그 트립와이어, 티어
   후보 큐(승격 전제 조건 포함), 승인 대기 중인 taxonomy 변경(엣지 등).
2. 다음 세션의 Claude는 이 필드를 **PACK을 열어** 읽고 진입한다. memory가 아니라
   PACK이 continuity carrier다. 새 대화는 "다시 시작"이 아니라 PACK의 이어달리기다.
3. 이 필드는 append로 자란다. 직전 부채가 해소되면 "RESOLVED"로 표기하되 지우지 않는다
   (감사 추적).

---

# 4. 업데이트 요청 방법 (사용자 → Claude 트리거)

일관성은 요청 형식에서도 나온다. 아래 형태로 요청하면 위 규율이 자동 발동된다.

**A. 정식 월간 갱신 (전면):**
```
아틀라스 업데이트
+ 최신 ATLAS_PACK.json 첨부 (필수)
+ (있으면) 이번에 새로 나온 신호/뉴스/자료
```
→ 51종 전면스캔 + §3A 전파 + 직전 부채(peak_watch·후보 큐·엣지 승인) 정산까지.

**B. 딥다이브 통합 (바텀업 되먹임):**
```
아틀라스 업데이트 — 딥다이브 통합
+ 최신 ATLAS_PACK.json 첨부
+ 딥다이브 DATA_PACK 파일들 (개별 기업 팩)
```
→ 노드 §7A 승격/신설 + 딥다이브 산업 인사이트만 톱다운 반영. 전면스캔은 안 함
   (부채로 명시). 오늘(UPDATE-2)이 이 유형이었다.

**C. 조회:**
```
자유 질문 + 최신 ATLAS_PACK.json 첨부
```
→ PACK 기반 답변, 갱신 없음.

**공통:** PACK 없이 갱신 요청 시 Claude는 정확도 저하를 고지하고 최신 PACK 첨부를
요구한다(00 §1). "일관성 있게 해줘" 한마디를 덧붙이면 Claude는 이 문서(03)의 진입
게이트를 명시적으로 밟는다.

---

# 4A. 스탠스 리프레시 (v1.4 — 매 갱신 공통 스텝)

A타입·B타입 모두 수행한다. **판단 작업이 아니라 관측값 갱신 작업**이므로,
티어 스캔·전파와 **분리된 마지막 단계**에서 실행한다.

```
STEP S1  DEEP_DIVED 노드의 stance 구조가 없으면 stance_zone 원문에서 구조화
         (parse_confidence 병기. 추출 불가 시 UNKNOWN — 추정 금지)
STEP S2  각 노드의 관측 주가를 갱신하고 as_of·source를 병기
STEP S3  zone·gap·freshness·tripwire_status·display_suppressed 재산출
STEP S4  01 §8 스탠스 게이트 통과 확인
```

**S1은 자동 파싱으로 끝나지 않는다.** `stance_zone`은 자유 서술이라 정규식이
있는 밴드를 놓치거나 잘못 뽑는다. (2026-09-02 시험 파싱: UNKNOWN 판정 7건 중
**4건은 원문에 밴드가 실제로 존재**했고 추출만 실패한 경우였다.)
따라서 파싱 결과는 **초안이며, 원문 대조 눈검수를 거친 뒤에만 PACK에 기록한다.**
이는 §1-4 "하드넘버는 원본 대조 후 반영"의 스탠스 레이어 적용이다.
검수 없이 기록된 밴드는 딥다이브 판단을 아틀라스가 왜곡한 것이므로 00 §0 위반이다.

**순서가 규율이다.** 스탠스 리프레시를 티어 판정보다 **먼저** 하면, 갱신된 가격이
그 세션의 티어 판단에 인지적으로 스며든다. 방화벽은 필드 참조만 막을 뿐 판단 순서는
막지 못하므로, **절차 순서로 막는다.** 티어·전파·후보 등재가 전부 확정된 뒤에 S1~S4를 연다.

**주가 소스 규율:**
- 소스는 세션마다 달라도 되지만 `source` 필드에 반드시 남긴다.
- 확인 못 한 노드는 이전 값을 유지하지 않고 `zone=UNKNOWN`으로 내린다.
  **낡은 가격을 최신인 척 두는 것이 가장 나쁘다.**
- 주가 갱신만으로는 diff 리포트의 티어·후보 섹션에 아무것도 쓰지 않는다(01 §5A).

---

# 5. UPDATE 세션 종료 체크리스트 (매 갱신 확정 前)

- [ ] 실물 PACK·repo 트리를 memory 아닌 파일로 확인했는가 (§1-1)
- [ ] tier/스키마/범위 변경 판단을 §0 미션과 대조했는가 (§1-2)
- [ ] 깊이 통합 시 §3A 전파를 실행했거나 미실행 범위를 부채로 남겼는가 (§1-3)
- [ ] 카드/deep_dive_ref/선행지표의 하드넘버를 원본 팩과 대조했는가 (§1-4)
- [ ] 04 확정 원칙 3줄(얕게=폭 / tier=정성 / 양방향 루프)을 위반한 변경이 0인가 (§2)
- [ ] `coverage_honesty`에 다음 세션 인수인계를 남겼는가 (§3)
- [ ] 티어 변경분이 전부 §3 트리거 2개 이상 근거를 갖는가 (00/01)
- [ ] 직전 카드 본문(수요/공급 서술)을 부주의하게 덮어쓴 곳이 0인가 (02 §4)
- [ ] 산출물은 새 PACK + diff 리포트이며, PACK만 교체하면 4개 뷰가 반영되는가
- [ ] **(v1.4) 스탠스 리프레시를 티어 확정 이후에 실행했는가 (§4A)**
- [ ] **(v1.4) 티어 변경 근거에 가격·zone 어휘가 0건인가 (00 §0 방화벽)**

---

## 부록: repo 반영 (실물 구조 기준)

실제 트리:
```
/data
  /deepdive        ← 딥다이브 개별 DATA_PACK 팩 보관
  ATLAS_PACK.json  ← 단일 원본. 이것만 교체하면 4개 뷰 자동 반영
ATLAS_CONSOLE.html
ATLAS_GRAPH.html
ATLAS_GUIDE.html
ATLAS_MAP.html
README.md
index.html
```

갱신 반영:
1. `data/ATLAS_PACK.json` 덮어쓰기 (뷰 반영은 이것만으로 끝)
2. `data/deepdive/`에 이번 딥다이브 팩 업로드
3. diff 리포트는 뷰가 fetch하지 않는 순수 기록물 — repo 필수 아님(로컬/`data/` 보관)
4. 커밋 → Pages 재빌드 1~2분 → 브라우저 하드 리프레시(Ctrl/Cmd+Shift+R)
5. **신규 폴더 만들지 않는다.** 기존 두 경로(`data/`, `data/deepdive/`)에만 얹는다.
